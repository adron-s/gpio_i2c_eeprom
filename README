Это код для чтения i2c eeprom флешки подключенной к gpio пинам.
Чтение осуществляется методом ногодрыга.

код сначала писался под Atmega 2560 а потом переделывался под ядро Openwrt и tplink tl-wr841n-v9
у tplink-а использовались следующие пины gpio: SDA(LAN1) := 16, SCK(LAN3) := 14
лампочки я не выпаивал. просто подключился до 220 ом резистора и подтянул на +2.65V через 10ком резистор.

Собственно это делалось для чтения eeprom микросхемы свитча tl-sg1008d. Помимо еепромки через это же соединение
можно читать и регистры свитча(SMI интерфейс). Но это уже другой проект.
